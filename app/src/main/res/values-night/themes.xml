<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MLDex" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/dark_blue</item>
    </style>

    <style name="Theme.MLDex" parent="Base.Theme.MLDex">
        <!-- Paksa ActionBar tetap Dark Blue di Night Mode -->
        <item name="android:actionBarStyle">@style/Widget.App.ActionBar</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Widget.App.ActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">@color/colorPrimary</item>
        <item name="titleTextStyle">@style/Widget.App.ActionBar.Title</item>
    </style>

    <style name="Widget.App.ActionBar.Title" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
