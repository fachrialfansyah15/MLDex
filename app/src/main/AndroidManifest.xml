<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MLDex">

        <!-- SplashActivity tanpa Action Bar -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MLDex.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login tanpa Action Bar -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.MLDex.NoActionBar"/>

        <!-- Register tanpa Action Bar -->
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.MLDex.NoActionBar"/>

        <!-- Welcome tanpa Action Bar -->
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/Theme.MLDex.NoActionBar"/>

        <!-- ListHero TETAP ada Action Bar (untuk menu titik tiga) -->
        <activity android:name=".ListHero"/>

        <!-- Detail Hero -->
        <activity android:name=".HeroDetailActivity"/>

    </application>

</manifest>
